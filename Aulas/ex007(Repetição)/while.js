// Esta é uma estrutura de repetiçõa com teste lógico no inicio

var c = 1

//enquanto a variavel c for menor que 8 escreva passo ${c} 
while(c <= 8){
    console.log(`passo ${c}`)
    c++ // adiciona a variavel c uma unidade a cada vez que o comando é executado
}
